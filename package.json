{
  "name": "rapiddigital.net",
  "version": "1.0.0",
  "description": "Rapid Digital website",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "rimraf dist",
    "copy": "mkdir -p dist && cp index.html dist/ && cp styles.css dist/ && cp *.png dist/ && mkdir -p dist/payment && cp payment.html dist/payment/index.html",
    "deploy": "npm run build"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
} 