{
  "name": "rapiddigital.net",
  "version": "1.0.0",
  "description": "Rapid Digital website",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "rimraf dist",
    "copy": "mkdir -p dist && cp index.html dist/ && cp *.png dist/",
    "deploy": "npm run build && aws s3 sync dist/ s3://rapiddigital.net --delete --acl public-read"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
} 